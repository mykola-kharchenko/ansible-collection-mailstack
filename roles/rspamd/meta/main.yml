---
galaxy_info:
  author: Mykola Kharchenko
  description: Install Rspamd with milter, DKIM signing and Valkey wiring
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: Fedora
      versions:
        - all
    - name: EL
      versions:
        - "9"
        - "10"
  galaxy_tags:
    - mail
    - rspamd
    - milter
    - dkim
    - antispam
# Requires the valkey role at runtime; ordered explicitly in the playbooks
# rather than as a hard dependency so image builds stay flat and predictable.
dependencies: []
